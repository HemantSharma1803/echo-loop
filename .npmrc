# GitHub Actions/npm should resolve the project's Vite plugin peer graph deterministically.
# The installed Vite 8 / plugin-react 6 stack is compatible; this avoids npm's strict peer resolver
# rejecting optional peer packages that are not required by this static game.
legacy-peer-deps=true
fund=false
audit=false
